extends Node

# NG+ must never rewrite canonical character rules.
# The three skill trees remain mutually exclusive in every cycle, and
# bosses/minibosses do not become capturable simply because NG+ is active.

func _ready() -> void:
    pass
