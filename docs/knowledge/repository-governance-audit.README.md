# Repository governance audit

The canonical repository policy is defined in `docs/knowledge/repository-governance-policy.json`.

The automated audit can prove whether `main` is protected by branch protection or an active branch ruleset. It cannot create or relax GitHub administration rules. Repository-admin configuration remains a separate infrastructure action.
