# Repository governance audit

The canonical policy is `docs/knowledge/repository-governance-policy.json`.

The audit proves whether `main` is protected by server-side GitHub controls. It never creates, weakens, or bypasses repository administration rules. Enabling the actual ruleset or branch protection remains a separate repository-admin action.
