# Carte de commandes PC

```bash
git checkout main
git pull
python tools/blender/vertical_slice_session.py --preflight
python tools/blender/vertical_slice_session.py --print-commands
```

Les commandes imprimées servent de base pour Darius, Goule puis arène. L'export final reste soumis au gate de validation.
