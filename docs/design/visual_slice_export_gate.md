# Gate d'export GLB

L'export vers les chemins consommés automatiquement par Godot n'est autorisé qu'après trois contrôles : rapport structure/budget 3D valide, revue visuelle approuvée, et absence d'erreur bloquante dans le preflight de session. Cette règle évite qu'un fichier incomplet remplace un proxy fiable.
